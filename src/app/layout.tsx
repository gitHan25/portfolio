import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "@/components/theme-provider";
import { Toaster } from "sonner";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Muhammad Farhan Editya - Portfolio",
  description:
    "Junior Backend Developer & Data Analytics Enthusiast pursuing Informatics degree. Passionate about backend technologies and data-driven development.",
  keywords: [
    "portfolio",
    "backend developer",
    "data analytics",
    "informatics",
    "junior developer",
    "data structures",
    "algorithms",
  ],
  authors: [{ name: "Muhammad Farhan Editya" }],
  creator: "Muhammad Farhan Editya",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster position="bottom-right" richColors />
        </ThemeProvider>
      </body>
    </html>
  );
}
